[project]
name = "example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.15.7",
]

[project.scripts]
start = "main:main"

[tool.uv]
package = true 